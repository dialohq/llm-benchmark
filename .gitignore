# venvs (rebuild via `uv sync`)
.venv*/
**/.venv-*/

# host-side snapshot dir, not ours
.venv-backups/

# HF model cache (~100GB+)
.hf_home/

# python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# logs / scratch
/tmp/
*.log

# benchmark inputs / outputs (kept locally)
.data/

# nix-ld's ldconfig wrapper drops mktemp files in cwd
tmp.??????????

# Rust build outputs
proxy/target/

# Local Claude Code state
.claude/
